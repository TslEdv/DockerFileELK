    - name: Install Docker
      apt:
        name:
          - docker.io
        state: present

    - name: Install Docker python
      apt:
        name: python3-docker
        state: present

    - name: Install sshpass package
      become: true
      apt:
        name: sshpass
