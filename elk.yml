---
  - name: Docker
    hosts: localhost
    roles:
      - docker
    tags: docker

  - name: Create VMs
    hosts: localhost
    roles:
      - compose
    tags: compose
  
  - name: Elasticsearch
    become: true
    hosts: elasticsearch
    roles:
      - elasticsearch
    tags: elastic